package KeyBinding;

public class Main {
	public static void main(String[] args) {
		// KeyBinding - bind an action to a keystrokes don't require you to click a component to give it focus 
		//              all swing component use key binding increased flexibility compared to key listener can assign
		//              key strokes to individual swing component more difficult to utilize and set up :( 
		
		new Game();
	}
}
